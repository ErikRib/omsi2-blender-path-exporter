name: Build APK

on:
  push:
    branches: [claude/mobile-learning-app-s4fxvr]
    paths:
      - 'kivy_app/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache Buildozer global directory
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: buildozer-py311-${{ hashFiles('kivy_app/buildozer.spec') }}-v4
          restore-keys: |
            buildozer-py311-

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            git zip unzip openjdk-17-jdk \
            autoconf libtool pkg-config \
            zlib1g-dev libncurses5-dev libncursesw5-dev \
            cmake libffi-dev libssl-dev build-essential \
            python3-dev

      - name: Install Buildozer and Cython
        run: pip install buildozer==1.5.0 "cython<3"

      - name: Build APK
        working-directory: kivy_app
        env:
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
        run: buildozer android debug

      - name: List generated files
        run: ls -lh $GITHUB_WORKSPACE/kivy_app/bin/ || echo "bin/ not found"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: aprenda-plus-debug
          path: kivy_app/bin/*.apk
          if-no-files-found: error
          retention-days: 30
